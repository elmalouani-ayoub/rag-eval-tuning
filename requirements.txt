# Dependances du coeur hors-ligne (sweep + evaluation, aucun telechargement de modele)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
PyYAML>=6.0

# --- Backends "production" optionnels (a activer sur ta machine) ---
# requests>=2.31              # necessaire pour les backends Ollama (embeddings + LLM)
# sentence-transformers>=2.2  # embeddings semantiques HuggingFace
# faiss-cpu>=1.7              # index vectoriel scalable
# ragas>=0.1                  # metriques de generation (fidelite/pertinence)
