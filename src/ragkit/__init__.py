"""ragkit — une boite a outils minimale pour construire et EVALUER un systeme RAG.

Le point fort du projet n'est pas le pipeline (volontairement simple et lisible),
mais le harnais d'evaluation qui mesure l'impact des hyperparametres
(taille de chunk, overlap, top_k, methode d'embedding) sur la qualite du retrieval.
"""

__version__ = "1.0.0"
